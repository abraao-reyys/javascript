let a = [5, 9, 1, 4, 2, 6, 4, 1, 3]

// Procurando valores

let index = a.indexOf(4)
console.log(index)

// Se o valor não existir

let index1 = a.indexOf(8)
console.log(index1) // Quando não existe, o valor -1 é retornado; pois não existe index -1
